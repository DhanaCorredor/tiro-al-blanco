// Plantilla de configuración de APIs.
//
// 1. Copia este archivo en la misma carpeta y renómbralo a `config.js`.
// 2. Reemplaza el valor de `apiKey` con tu propia clave de GNews
//    (regístrate gratis en https://gnews.io/).
// 3. `config.js` está en .gitignore: nunca se subirá al repo público.

window.NEWS_CONFIG = {
  apiKey: 'TU_API_KEY_DE_GNEWS',
  defaultQuery: 'tecnologia',
  maxResults: 3
};
